# Ten LOKALNY .env jest OPCJONALNY. Dane logowania do bazy i ERP trzymamy
# w JEDNYM wspólnym pliku: automatyzacje/.env (patrz automatyzacje/.env.example).
# scrape.js czyta najpierw ten lokalny plik (jeśli istnieje), a brakujące
# wartości dobiera ze wspólnego. Twórz ten plik tylko, jeśli PZ ma mieć INNE
# dane niż reszta automatyzacji — inaczej wystarczy wspólny automatyzacje/.env.
#
# Sekrety (odkomentuj i wpisz TYLKO jeśli nadpisujesz wspólne):
# DB_HOST=
# DB_PORT=1433
# DB_USER=
# DB_PASSWORD=
# DB_NAME=
# ERP_LOGIN=
# ERP_PASSWORD=

# Opcjonalne — mają rozsądne wartości domyślne w scrape.js, ustaw tylko żeby nadpisać.
# MAX_DOCS=5           # TRYB OSTROŻNY (próbka). Po zatwierdzeniu próbki: 900.
# MIN_SESSION_MINUTES=30
# MAX_SESSION_MINUTES=60
# BATCH_SIZE=15
# HEADLESS=true
# FILTER_DATE=2026-08-03
# USE_DOC_TYPE_FILTER=true    # domyślnie włączony — zapisany filtr "PZ" istnieje w ERP (od 2026-09-23)
# PZ_DOC_TYPE_FILTER_LABEL=PZ # nazwa zapisanego filtra do kliknięcia
# Brak PZ_LIST_URL — w przeciwieństwie do WZ/MM nawigacja do listy PZ idzie
# WYŁĄCZNIE przez klik w menu (Logistyka -> Przychody zewnętrzne), bezpośredni
# page.goto() na tę stronę nie działa. Patrz navigateToPzList() w scrape.js.
